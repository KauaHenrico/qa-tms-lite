{
  "name": "tms-lite",
  "version": "1.0.0",
  "private": true,
  "description": "TMS Lite - gestao de entregas",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "cypress:run": "cypress run",
    "cypress:open": "cypress open",
    "test:e2e": "start-server-and-test start http-get://127.0.0.1:3000 cypress:run"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "cypress": "^15.20.1",
    "start-server-and-test": "^3.0.12"
  }
}
